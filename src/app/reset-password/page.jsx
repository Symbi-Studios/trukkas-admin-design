import { ResetPasswordScreen } from '../../screens/AuthScreens.jsx';

export const metadata = { title: 'Reset password', referrer: 'no-referrer' };

export default function ResetPasswordPage() {
  return <ResetPasswordScreen />;
}
