import { SettingsPasswords } from '../../../screens/SettingsPasswords.jsx';

export const metadata = {
  title: 'Password & Sessions',
  description: 'Manage password requirements, user sessions, and login behavior for a secure platform.',
};

export default function SettingsPasswordsPage() {
  return <SettingsPasswords />;
}
