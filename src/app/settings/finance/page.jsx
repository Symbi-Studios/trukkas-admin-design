import { SettingsFinance } from '../../../screens/SettingsFinance.jsx';

export const metadata = {
  title: 'Finance Settings',
  description: 'Configure financial rules, fees, escrow, payouts, and accounting preferences.',
};

export default function SettingsFinancePage() {
  return <SettingsFinance />;
}
