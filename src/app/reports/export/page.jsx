import { ExportReport } from '../../../screens/ExportReport.jsx';

export const metadata = {
  title: 'Export Report',
  description: 'Configure your report preferences and export data in your preferred format.',
};

export default function ExportReportPage() {
  return <ExportReport />;
}
