Row of selectable scope tiles — "Organization Wide / Restricted to Branches / Custom Scope" on Create New Role.

```jsx
<ChoiceCard selected icon="globe" title="Organization Wide"
            description="Can access all data across the entire platform." />
```
