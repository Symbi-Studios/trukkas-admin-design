Single choice. The permission matrix on Create New Role is a grid of bare radios (no label) — one column per access level.

```jsx
<Radio checked={level === 'full'} onChange={() => set('full')} />
```
