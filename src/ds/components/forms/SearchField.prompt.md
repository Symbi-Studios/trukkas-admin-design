Two uses: the global field in the TopBar ("Search jobs, trucks, companies, exporters…") and the in-panel field above a table.

```jsx
<SearchField variant="global" placeholder="Search jobs, trucks, companies, exporters..." />
<SearchField placeholder="Search cargo types by name or description..." />
```
