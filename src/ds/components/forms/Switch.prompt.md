38×22 track, blue when on. Settings toggles carry a label plus a one-line explanation ("Require 2FA for users with this role").

```jsx
<Switch checked={on} onChange={setOn} label="Allow this role to manage users"
        hint="Can create, edit and deactivate user accounts." />
```
