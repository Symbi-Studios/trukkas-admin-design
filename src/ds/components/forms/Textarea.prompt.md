Multi-line field with the inline character counter used on "Describe the purpose and scope of this role…".

```jsx
<Textarea label="Description" maxLength={500} value={v} onChange={e => set(e.target.value)} />
```
