Opens every page. The description is one plain sentence — "Manage all cargo types used across jobs and shipments."

```jsx
<PageHeader crumbs={['Administration','User Management']} title="User Management"
  description="Manage users, view their roles, permissions and account status."
  actions={<><Button variant="outline" icon="upload">Import Users</Button>
             <Button variant="accent" icon="plus">Add New User</Button></>} />
```
