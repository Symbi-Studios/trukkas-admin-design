The 64px application bar. No shadow, one hairline underneath. Search placeholder names the records searchable on that module.

```jsx
<TopBar onMenu={toggle} notifications={12} user="Trukkas Admin" role="Super Admin" />
```
