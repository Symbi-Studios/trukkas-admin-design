Fleet Status, Users by Role, Jobs by Status — always a donut with a centred total and a LegendList beside it, never labels on the arc.

```jsx
<DonutChart data={[{label:'In Transit',value:10},{label:'Loading',value:5}]} centerValue="24" centerLabel="Total Trucks" />
```
