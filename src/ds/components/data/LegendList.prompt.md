Always paired with DonutChart or LineChart. Colour never travels alone — every dot sits beside a word and a number.

```jsx
<LegendList items={[{label:'In Transit',value:10},{label:'Loading',value:5}]} />
```
