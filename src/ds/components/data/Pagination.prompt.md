Footer of every list card: the count line on the left, numbered pages centre-right, rows-per-page last. The current page is a solid blue square.

```jsx
<Pagination page={1} pageCount={125} total={1248} onPage={setPage} onPageSize={setSize} />
```
