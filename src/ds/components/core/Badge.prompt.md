Status is a controlled vocabulary — pass the label and the badge picks its own tone from `TK_STATUS`. 22px tall, 6px radius, soft fill, never a solid one.

```jsx
<Badge>In Transit</Badge>
<Badge dot>On Trip</Badge>
<Badge tone="danger">Expired</Badge>
```
