Primary action with an attached caret for its variants — "Add Cargo Type ▾", "Upload Document ▾", "Send Reply ▾".

```jsx
<SplitButton icon="plus" onAction={create} onToggle={openMenu}>Add Cargo Type</SplitButton>
```
