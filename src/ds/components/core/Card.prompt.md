Base surface: 14px radius, white, one hairline border, near-invisible shadow. Nested blocks inside a card drop to `tone="sunk"`.

```jsx
<Card><SectionCard.Header title="Fleet Utilization" />…</Card>
<Card tone="navy" pad="md">Total Wallet Balance …</Card>
```
