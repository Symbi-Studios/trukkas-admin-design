Every titled panel on a console page — "Recent Jobs", "Fleet Utilization", "Maintenance Alerts". Use `pad="none"` when the body is a DataTable so rows reach the card edge.

```jsx
<SectionCard title="Recent Events" footer={<a href="#">View all events</a>}>…</SectionCard>
```
