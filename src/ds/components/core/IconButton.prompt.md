Square icon-only control: the row overflow "⋮", the eye preview in a table, the map expand, the top-bar bell.

```jsx
<IconButton icon="ellipsis-vertical" label="Row actions" />
<IconButton icon="eye" tone="outline" label="Preview" />
```
