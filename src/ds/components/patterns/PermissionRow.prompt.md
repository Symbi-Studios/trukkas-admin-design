A module row in the Create/Edit Role permission matrix. Columns are Full Access, Custom Access, No Access — one radio each.

```jsx
<PermissionRow icon="layout-dashboard" module="Dashboard" value="full" onChange={set} />
<PermissionRow icon="wallet" module="Finance" actions={6} value="custom" onChange={set} />
```
