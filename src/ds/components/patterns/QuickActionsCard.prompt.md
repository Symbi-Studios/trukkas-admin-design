Shortcuts panel in the right rail. Every console detail page carries one; the dashboard uses the 3-across grid form.

```jsx
<QuickActionsCard items={[{ icon: 'file-plus', label: 'Create Job for this Forwarder' },
                          { icon: 'shield-check', label: 'Add Verification Request' }]} />
```
