'use client';
import { useMemo,useState } from 'react';
import { useNavigate } from '../router.js';
import { PageHeader,Button,StatCard,SectionCard,Tabs,SearchField,DataTable,Pagination,Badge,Icon,Modal,TextField,Select } from '../ds.js';
import { useCollection } from '../mock/useCollection.js';
import { createDemurrage } from '../mock/api.js';
import './Demurrage.css';
const PAGE_SIZE=8;const STATUS_TONE={Active:'danger',Upcoming:'warning',Resolved:'success',Dispute:'purple'};const money=n=>'₦'+Number(n||0).toLocaleString('en-NG',{minimumFractionDigits:2});
function exportCsv(rows){const csv=[['Job ID','Container','Vessel','Customer','Free Days','Days Used','Charge','Status','Days Over'],...rows.map(r=>[r.jobId,r.container,r.vessel,r.customer,r.freeDays,r.daysUsed??'',r.charge,r.status,r.daysOver??''])].map(x=>x.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(',')).join('\n');const u=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));const a=document.createElement('a');a.href=u;a.download='demurrage-report.csv';a.click();URL.revokeObjectURL(u)}
export function Demurrage(){const navigate=useNavigate(),rows=useCollection('demurrage')||[];const [tab,setTab]=useState('All Demurrage'),[q,setQ]=useState(''),[status,setStatus]=useState('All'),[jobType,setJobType]=useState('All'),[date,setDate]=useState(''),[page,setPage]=useState(1),[pageSize,setPageSize]=useState(PAGE_SIZE),[period,setPeriod]=useState('This Month'),[create,setCreate]=useState(false),[rules,setRules]=useState(false),[form,setForm]=useState({jobId:'',container:'',customer:'',freeDays:7,daysOver:1,rate:75000}),[toast,setToast]=useState('');
 const filtered=useMemo(()=>rows.filter(r=>(tab==='All Demurrage'||r.status===tab.replace(/s$/,''))&&(status==='All'||r.status===status)&&(jobType==='All'||r.jobType===jobType)&&(!q||[r.jobId,r.container,r.customer,r.vessel].some(v=>v.toLowerCase().includes(q.toLowerCase())))&&(!date||r.chargeStart.includes(new Date(date+'T12:00:00').toLocaleDateString('en-US',{month:'short',day:'numeric'})))),[rows,tab,status,jobType,q,date]);const paged=filtered.slice((page-1)*pageSize,page*pageSize);function notify(s){setToast(s);setTimeout(()=>setToast(''),1600)}async function submit(){if(!form.jobId||!form.container||!form.customer)return;const r=await createDemurrage({...form,freeDays:+form.freeDays,daysOver:+form.daysOver,rate:+form.rate});setCreate(false);navigate('/demurrage/'+r.id)}
 return <div className="dem-page"><PageHeader crumbs={['Finance','Demurrage']} title="Demurrage" description="Track and manage demurrage and detention charges across all shipments and containers." actions={<Button icon="download" onClick={()=>exportCsv(filtered)}>Export Report</Button>}/><div className="dem-layout"><div className="dem-main"><div className="dem-stats"><StatCard icon="calendar-x" tint="red" label="Active Demurrage" value="27" caption="₦18,645,000.00 Total Charges"/><StatCard icon="calendar-clock" tint="amber" label="Upcoming Charges" value="16" caption="₦7,250,000.00 Est. Total Charges"/><StatCard icon="inbox" label="Resolved (This Month)" value="42" caption="₦32,280,000.00 Total Resolved"/><StatCard icon="circle-check" tint="green" label="Collected (This Month)" value="₦24,680,000.00" delta="68.4%" caption="Collection Rate"/><StatCard icon="circle-alert" tint="purple" label="Disputes" value="4" caption="₦2,150,000.00 In Dispute"/></div><SectionCard title="" pad="none"><Tabs value={tab} onChange={v=>{setTab(v);setPage(1)}} items={[{label:'All Demurrage',value:'All Demurrage'},{label:'Active',value:'Active',count:27},{label:'Upcoming',value:'Upcoming',count:16},{label:'Resolved',value:'Resolved',count:42},{label:'Disputes',value:'Disputes',count:4}]} style={{padding:'0 16px'}}/><div className="dem-toolbar"><SearchField className="search" style={{flex:1}} placeholder="Search by job ID, container no., customer..." value={q} onChange={e=>{setQ(e.target.value);setPage(1)}}/><select className="dem-select" aria-label="Status filter" value={status} onChange={e=>{setStatus(e.target.value);setPage(1)}}>{['All','Active','Upcoming','Resolved','Dispute'].map(x=><option key={x}>{x}</option>)}</select><select className="dem-select" aria-label="Job type filter" value={jobType} onChange={e=>{setJobType(e.target.value);setPage(1)}}><option>All</option>{[...new Set(rows.map(r=>r.jobType))].map(x=><option key={x}>{x}</option>)}</select><input className="dem-input" aria-label="Date range" style={{width:145}} type="date" value={date} onChange={e=>setDate(e.target.value)}/><Button variant="outline" icon="rotate-ccw" onClick={()=>{setQ('');setStatus('All');setJobType('All');setDate('');setPage(1)}}>Reset</Button></div><DataTable rows={paged} rowKey={r=>r.id} onRowClick={r=>navigate('/demurrage/'+r.id)} columns={[{key:'jobId',header:'Job ID',render:r=><span className="dem-title-cell"><strong>{r.jobId}</strong><small>{r.jobType}</small></span>},{key:'container',header:'Container No.',render:r=><span className="dem-double"><strong>{r.container}</strong><small>{r.size}</small></span>},{key:'vessel',header:'Vessel / Voyage',render:r=><span className="dem-double"><strong>{r.vessel}</strong><small>{r.voyage}</small></span>},{key:'customer',header:'Customer',render:r=><span className="dem-double"><strong>{r.customer}</strong><small>{r.customerType}</small></span>},{key:'freeDays',header:'Free Days',render:r=><span className="dem-double"><strong>{r.freeDays}</strong><small>{r.freePeriod}</small></span>},{key:'daysUsed',header:'Days Used',render:r=><span className="dem-double"><strong>{r.daysUsed??'-'}</strong><small>{r.usedPeriod}</small></span>},{key:'charge',header:'Charge (₦)',render:r=><span className="dem-double"><strong>{money(r.charge)}</strong><small>{r.rate?money(r.rate)+'/day':r.status==='Upcoming'?'Within Free Days':r.overSince}</small></span>},{key:'status',header:'Status',render:r=><Badge tone={STATUS_TONE[r.status]}>{r.status}</Badge>},{key:'daysOver',header:'Days Over',render:r=><span className="dem-double"><strong style={{color:r.daysOver?'var(--tk-danger)':undefined}}>{r.daysOver??'-'}{r.daysOver!=null?' days':''}</strong><small>{r.overSince}</small></span>},{key:'x',header:'Action',render:r=><Button variant="ghost" size="sm" icon="ellipsis" onClick={e=>{e.stopPropagation();navigate('/demurrage/'+r.id)}}/>}]}/><Pagination page={page} pageCount={Math.max(1,Math.ceil(filtered.length/pageSize))} pageSize={pageSize} total={filtered.length} onPage={p=>setPage(Math.min(Math.max(1,p),Math.max(1,Math.ceil(filtered.length/pageSize))))} onPageSize={n=>{setPageSize(n);setPage(1)}}/></SectionCard></div>
 <div className="dem-rail"><SectionCard title="Demurrage Summary" action={<select aria-label="Summary period" className="dem-select" value={period} onChange={e=>setPeriod(e.target.value)}><option>This Month</option><option>Last Month</option><option>This Year</option></select>}><div className="dem-summary"><div className="dem-ring"/><div className="dem-legend"><span><i style={{color:'#f14b67'}}>● Active</i><b>₦18.6M (72%)</b></span><span><i style={{color:'#f2b400'}}>● Upcoming</i><b>₦7.3M (14%)</b></span><span><i style={{color:'#0da56a'}}>● Resolved</i><b>₦32.3M (12%)</b></span><span><i style={{color:'#762ce8'}}>● Disputes</i><b>₦2.2M (2%)</b></span></div></div><div className="dem-summary-totals"><div className="dem-info-line"><span>Total Charges</span><b>₦25,895,000.00</b></div><div className="dem-info-line"><span>Total Collected</span><b>₦24,680,000.00</b></div><div className="dem-info-line"><span>Outstanding</span><b>₦1,215,000.00</b></div><div className="dem-info-line"><span>Collection Rate</span><b>93.2%</b></div></div></SectionCard><SectionCard title="Quick Actions"><Action icon="clipboard-plus" title="Create Demurrage Charge" hint="Manually create a demurrage charge" onClick={()=>setCreate(true)}/><Action icon="clipboard-list" title="Demurrage Rules" hint="Manage free days and rate rules" onClick={()=>setRules(true)}/><Action icon="calculator" title="Bulk Demurrage Calculation" hint="Calculate demurrage for multiple jobs" onClick={()=>notify('Bulk calculation completed for visible records')}/><Action icon="file-chart-column" title="Demurrage Reports" hint="View and export demurrage reports" onClick={()=>exportCsv(filtered)}/></SectionCard><SectionCard title="Recent Demurrage" action={<button style={{border:0,background:'none',color:'var(--tk-blue)',cursor:'pointer'}} onClick={()=>{setTab('All Demurrage');setPage(1)}}>View all</button>}>{rows.slice(0,4).map(r=><button className="dem-recent" style={{width:'100%',borderLeft:0,borderRight:0,borderTop:0,background:'transparent',cursor:'pointer',textAlign:'left'}} key={r.id} onClick={()=>navigate('/demurrage/'+r.id)}><span><strong>{r.jobId}</strong><br/><Badge tone={STATUS_TONE[r.status]} style={{height:17,fontSize:8}}>{r.status}</Badge></span><span>{r.container}<br/><small>{r.dischargeDate}</small></span><span style={{textAlign:'right'}}><b>{money(r.charge)}</b><br/><small>{r.daysOver?r.daysOver+' days over':r.overSince}</small></span></button>)}</SectionCard></div></div>
 <Modal open={create} onClose={()=>setCreate(false)} title="Create Demurrage Charge" width={580} footer={<><Button variant="outline" onClick={()=>setCreate(false)}>Cancel</Button><Button disabled={!form.jobId||!form.container||!form.customer} onClick={submit}>Create Charge</Button></>}><div className="dem-form"><div className="dem-form-grid"><TextField label="Job ID" value={form.jobId} onChange={e=>setForm({...form,jobId:e.target.value})}/><TextField label="Container Number" value={form.container} onChange={e=>setForm({...form,container:e.target.value})}/></div><TextField label="Customer" value={form.customer} onChange={e=>setForm({...form,customer:e.target.value})}/><div className="dem-form-grid"><TextField label="Free Days" type="number" value={form.freeDays} onChange={e=>setForm({...form,freeDays:e.target.value})}/><TextField label="Days Over" type="number" value={form.daysOver} onChange={e=>setForm({...form,daysOver:e.target.value})}/></div><TextField label="Daily Rate (₦)" type="number" value={form.rate} onChange={e=>setForm({...form,rate:e.target.value})}/></div></Modal><Modal open={rules} onClose={()=>setRules(false)} title="Demurrage Rules" description="Default free-time and daily charge settings." width={520} footer={<Button onClick={()=>{setRules(false);notify('Demurrage rules saved')}}>Save Rules</Button>}><div className="dem-form-grid"><TextField label="Default Free Days" type="number" defaultValue="7"/><TextField label="Default Daily Rate (₦)" type="number" defaultValue="75000"/></div></Modal>{toast&&<div className="dem-toast">{toast}</div>}</div>}
function Action({icon,title,hint,onClick}){return <button className="dem-action" onClick={onClick}><span className="ico"><Icon name={icon} size={13}/></span><span><strong>{title}</strong><small>{hint}</small></span><Icon name="chevron-right" size={13}/></button>}
